<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pueblos"
        tools:targetApi="31">

        <!-- RegisterActivity -->
        <activity android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity android:name=".MainActivity"
            android:exported="true"/>

        <!-- LoginActivity -->
        <activity android:name=".LoginActivity"
            android:exported="true" />

        <activity android:name=".InfoActivity"
            android:exported="true" />

        <activity android:name=".InicioActivity"
            android:exported="true" />

        <activity android:name=".PasteActivity"
            android:exported="true" />

        <activity android:name=".FeriaActivity"
            android:exported="true" />

        <activity android:name=".AtractivosActivity"
            android:exported="true" />

        <activity android:name=".Parque_penasActivity"
            android:exported="true" />

        <activity android:name=".HilocheActivity"
            android:exported="true" />

        <activity android:name=".CascadaBanActivity"
            android:exported="true" />

        <activity android:name=".MinaActivity"
            android:exported="true" />

        <activity android:name=".PanteonActivity"
            android:exported="true" />

        <activity android:name=".ZelontlaActivity"
            android:exported="true" />



    </application>

</manifest>
